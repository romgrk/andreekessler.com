# README

The HTML generation script is at `./src/content/generate.js`. It requires Node.js and NPM to run.

To install dependencies, run:

```
npm install
```

To generate the HTML file, run:

```
npm run generate
```
